{
  "name": "reproducible",
  "version": "0.0.2",
  "description": "A library for coordinating reproducible builds with docker.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Zachary Pelkey, Alec Wantoch",
  "license": "MIT",
  "dependencies": {
    "dockerode": "^3.3.0",
    "ipfs": "^0.55.4",
    "tar-fs": "^2.1.1"
  },
  "devDependencies": {
    "typescript": "^4.3.4"
  }
}
